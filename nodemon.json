{
  "ignore": ["**/*.test.ts", ".git", "node_modules", "lib"],
  "watch": ["src", "package.json"],
  "exec": "yarn build",
  "ext": "ts",
  "events": {
    "crash": "yarn notify -t 'oclif-template: nodemon' -m 'build failed'"
  }
}
